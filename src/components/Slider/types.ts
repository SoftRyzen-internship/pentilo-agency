export interface SliderProps {
  section: 'cases' | 'services';
  // pagination?: boolean;
  navigation?: boolean;
  autoplay?: boolean;
  // data: any;
  // element: any;
  allowTouchMove?: boolean;
  className?: string;
  slideClassName?: string;
}
